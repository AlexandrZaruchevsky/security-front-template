import { createStore } from 'vuex'
import storeAuth from './store-auth'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth: storeAuth
  }
})